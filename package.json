{
  "name": "fx-browser",
  "version": "0.1.1",
  "description": "FX Browser - Windows personal browser environment manager with MoreLogin import support.",
  "private": true,
  "main": "dist-electron/main/main.js",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "dev:electron": "concurrently -k \"npm:dev\" \"wait-on tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "build": "tsc -p tsconfig.json && vite build && npm run build:main",
    "build:main": "tsc -p tsconfig.electron.json && node scripts/copy-preload.mjs",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.electron.json --noEmit",
    "package:win": "npm run build && electron-builder --win --x64",
    "prepare:fingerprint-chromium": "node scripts/prepare-fingerprint-chromium.mjs",
    "lint:security": "node scripts/check-no-sensitive-files.mjs"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "proxy-chain": "^3.0.0",
    "react": "latest",
    "react-dom": "latest",
    "undici": "^8.5.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@types/ws": "^8.18.1",
    "@vitejs/plugin-react": "latest",
    "concurrently": "latest",
    "cross-env": "latest",
    "electron": "latest",
    "electron-builder": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest",
    "wait-on": "latest"
  }
}
